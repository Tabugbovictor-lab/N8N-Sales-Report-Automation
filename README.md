# N8N-Sales-Report-Automation
An automated sales reporting pipeline built in n8n. It captures booking notes via a custom form trigger, uses Google Gemini to extract key commercial insights (sentiment, pain points, deal values, and action items), parses the response using a JavaScript Code node, and logs structured rows into Google Sheets in real time.
